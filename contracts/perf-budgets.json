{
  "$commento": "Numeri, non aggettivi. La CI fallisce se un budget viene sforato. Rivedili con @performance alla tappa 05.",
  "$metodo_laboratorio_pagine_pubbliche": {
    "scopo": "Metodo di misura per lcp_ms_lab_mediana e cls_lab_mediana di pagine_pubbliche (AC39, DV-4). Non riguarda js_iniziale_gzip_kb e css_gzip_kb, misurati direttamente sull'artefatto della build, ne' lcp_ms_p75 e cls_p75, dati di campo.",
    "strumento": "Playwright, Chromium headless, contro la build di produzione servita in locale (astro preview). Nessun servizio terzo (no PageSpeed Insights, no WebPageTest). LCP e CLS letti con PerformanceObserver nativo del browser. LCP: ultima entry largest-contentful-paint osservata con buffered true, letta dopo levento load quando non arrivano piu' nuove entry (chiusura nota N2 di ADR-0001: prima di load il browser puo' ancora sostituire il candidato piu' grande). CLS: somma dei value delle entry layout-shift con hadRecentInput=false su tutta la sessione di caricamento della pagina. Nota (chiusura N1 di ADR-0001): questa somma non e' la definizione corrente di CLS di web.dev, che e' la finestra di sessione con il punteggio piu' alto (il burst piu' grande, con pause fra uno spostamento e laltro inferiori a 1 secondo e al massimo 5 secondi in tutto); la somma e' sempre maggiore o uguale a quella finestra, quindi la misura resta prudente e puo' bocciare una pagina che il CLS reale promuoverebbe, mai il contrario.",
    "dispositivo": "Viewport 360x640 CSS px (lo stesso profilo mobile usato per AC37), CPU throttling 4x applicato via CDP Emulation.setCPUThrottlingRate(4) - preset mobile di riferimento di Lighthouse.",
    "rete": "Profilo pubblico Slow 4G di Lighthouse (docs/throttling.md di Lighthouse), applicato via CDP Network.emulateNetworkConditions con i parametri richiesti dal protocollo: latency=150 (millisecondi), downloadThroughput=200000 (byte al secondo, da 1.6 Mbps con convenzione decimale, 1 Mbps = 1000000 bit al secondo), uploadThroughput=93750 (byte al secondo, da 750 Kbps con la stessa convenzione). Questi sono i valori pubblicati del preset a livello di pacchetto: Network.emulateNetworkConditions li applica per richiesta e non per pacchetto, quindi la latenza e il throughput indotti sono meno severi di quelli che Lighthouse produce con lo stesso preset nominale, perche' Lighthouse applica in piu' dei moltiplicatori di correzione per il proprio metodo di throttling 'devtools' (RTT per 3.75, throughput per 0.9). Risoluzione O1 di ADR-0001 (docs/adr/0001-budget-pagine-pubbliche-laboratorio.md): questo metodo NON adotta quei moltiplicatori, per due motivi. Primo, la fonte del valore 3.75/0.9 per la versione corrente di Lighthouse non e' verificata nel sorgente attuale (journal/2026-09-13/201807-architect-fallimento.json): i due numeri vengono da un commit storico, e che il profilo mobileSlow4G corrente li applichi ancora e' un dato letto in un riassunto, non nel codice; non si costruisce una soglia di contratto su un dato non verificato da chi lo dichiara. Secondo, la soglia lcp_ms_lab_mediana sotto non richiede quella premessa: vedi pagine_pubbliche, campo nota_soglie_lab.",
    "esecuzioni": 5,
    "statistica": "mediana delle 5 esecuzioni (p50); pratica raccomandata da Lighthouse/web.dev per ridurre il rumore di singola misura (eseguire almeno 3 volte, idealmente 5, e usare la mediana).",
    "chi_esegue": "L07 (tests/perf/**), sulla build di produzione. Non e' un dato di campo (RUM): e' una misura di laboratorio, ripetibile senza traffico reale.",
    "isolamento_esecuzioni": "Risoluzione O2 di ADR-0001. Ognuna delle 5 esecuzioni parte a freddo: nuovo browser.newContext() di Playwright per ogni esecuzione, senza storage, cookie o cache condivisi con le esecuzioni precedenti; cache HTTP vuota a ogni esecuzione, nessuna risposta riutilizzata dalla richiesta precedente; nessun service worker registrato (le pagine pubbliche non caricano JavaScript lato client, ADR-0002 D9, quindi non possono registrarne uno in questa consegna). Motivo: con cache calda le esecuzioni dalla seconda in poi non passerebbero dalla rete limitata, e la mediana misurerebbe il tempo dalla cache invece del caricamento a freddo che la soglia intende verificare."
  },
  "pagine_pubbliche": {
    "js_iniziale_gzip_kb": 60,
    "css_gzip_kb": 25,
    "lcp_ms_p75": 2000,
    "cls_p75": 0.05,
    "$nota_dati_di_campo": "lcp_ms_p75 e cls_p75 sono dati di campo (RUM, 75 percentile su traffico reale): non esistono e non si verificano prima del lancio (DV-4). Restano come obiettivo per quando ci sara' traffico; non sono rimossi ne' rinominati (contracts/README.md).",
    "lcp_ms_lab_mediana": 2500,
    "cls_lab_mediana": 0.05,
    "$nota_soglie_lab": "Soglie di laboratorio per AC39/DV-4, verificabili oggi senza traffico reale. Metodo in $metodo_laboratorio_pagine_pubbliche. LCP allineato alla soglia 'good' di Core Web Vitals (2500 ms); CLS allineato al target di campo (0.05), perche' il CLS non risente del throttling di rete/CPU come il LCP e queste pagine non hanno contenuto dinamico iniettato. Motivazione estesa in journal/2026-09-13/194653-performance-decisione.json. La soglia LCP puo' essere piu' larga del p75 di campo (2000) perche' il laboratorio misura sempre la condizione peggiore fissata a ogni esecuzione, mentre il p75 di campo aggrega anche condizioni migliori (ADR-0001, principio accettato); non perche' il profilo di rete dichiarato sia piu' severo di quello che genera il p75 di campo, premessa non verificata e non adottata (risoluzione O1 in metodo_laboratorio_pagine_pubbliche.rete). Chiusura di O1 e O2 in journal/2026-09-13/202805-performance-decisione.json."
  },
  "area_cliente": {
    "js_iniziale_gzip_kb": 180,
    "lcp_ms_p75": 2500
  },
  "api": {
    "registrazione_p95_ms": 800,
    "conteggio_richieste_p95_ms": 15,
    "query_per_richiesta_max": 4
  },
  "$lettura_ci": "La CI (L12, .github/workflows/site-ci.yml) legge le soglie per chiave da questo file e le confronta con l'output del test di laboratorio di L07 (tests/perf/**), che riporta i valori misurati con le stesse chiavi: pagine_pubbliche.js_iniziale_gzip_kb e pagine_pubbliche.css_gzip_kb dal peso della build; pagine_pubbliche.lcp_ms_lab_mediana e pagine_pubbliche.cls_lab_mediana dalla mediana di 5 esecuzioni con il metodo in $metodo_laboratorio_pagine_pubbliche. Il job fallisce se un valore misurato supera la soglia corrispondente. Le chiavi lcp_ms_p75 e cls_p75 non sono lette dalla CI in questa consegna: sono dati di campo non verificabili senza traffico reale (DV-4)."
}
