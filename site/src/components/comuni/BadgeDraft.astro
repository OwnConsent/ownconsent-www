---
// Contrassegno «bozza» (AC8): docs/design/00-sistema-e-componenti.md.
// Primo elemento dentro <main>, prima dell'h1, sulle tre pagine legali.
---
<p class="badge badge-draft">Bozza — testo non definitivo, in attesa di approvazione</p>
<style>
  .badge {
    display: inline-block;
    width: fit-content;
    border-radius: var(--radius-full);
    font-weight: var(--font-weight-semibold);
    font-size: var(--font-size-100);
    padding: var(--spacing-2) var(--spacing-4);
    margin: 0 0 var(--spacing-4);
  }

  .badge-draft {
    background: var(--color-badge-draft-bg);
    color: var(--color-badge-draft-text);
  }
</style>
